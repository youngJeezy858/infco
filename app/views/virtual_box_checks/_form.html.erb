<%= form_for(@virtual_box_check) do |f| %>
  <% if @virtual_box_check.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@virtual_box_check.errors.count, "error") %> prohibited this virtual_box_check from being saved:</h2>

      <ul>
      <% @virtual_box_check.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="controls">
    <%= f.label :ticket %>
    <div class="control-group">
      <%= f.number_field :ticket %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to "Back", virtual_box_checks_path, :class => 'btn' %>
  </div>
<% end %>
