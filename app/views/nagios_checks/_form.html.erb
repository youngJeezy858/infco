<%= form_for(@nagios_check) do |f| %>
  <% if @nagios_check.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nagios_check.errors.count, "error") %> prohibited this nagios_check from being saved:</h2>

      <ul>
      <% @nagios_check.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
