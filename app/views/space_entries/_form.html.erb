<%= form_for(@space_entry) do |f| %>
  <% if @space_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@space_entry.errors.count, "error") %> prohibited this space_entry from being saved:</h2>

      <ul>
      <% @space_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
