<%= form_for(@nagios_entry) do |f| %>
  <% if @nagios_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nagios_entry.errors.count, "error") %> prohibited this nagios entry from bei\
ng saved:</h2>

      <ul>
      <% @nagios_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="controls">
    <%= f.label :name %>
    <div class="control-group">
      <%= f.text_field :name %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to 'Back', nagios_entries_path, :class => 'btn' %>
  </div>
<% end %>
