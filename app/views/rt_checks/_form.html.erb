<%= form_for(@rt_check) do |f| %>
  <% if @rt_check.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rt_check.errors.count, "error") %> prohibited this rt_check from being saved:</h2>

      <ul>
      <% @rt_check.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
