<%= form_for(@printer_check) do |f| %>
  <% if @printer_check.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@printer_check.errors.count, "error") %> prohibited this printer_check from being saved:</h2>

      <ul>
      <% @printer_check.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
