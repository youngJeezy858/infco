<%= form_for(@load_balancer) do |f| %>
  <% if @load_balancer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@load_balancer.errors.count, "error") %> prohibited this load balancer from bei\
ng saved:</h2>

      <ul>
      <% @load_balancer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="controls">
    <%= f.label :name %>
    <div class="control-group">
      <%= f.text_field :name %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to 'Back', load_balancers_path, :class => 'btn' %>
  </div>
<% end %>
