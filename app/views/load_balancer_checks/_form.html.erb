<%= form_for(@load_balancer_check) do |f| %>
  <% if @load_balancer_check.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@load_balancer_check.errors.count, "error") %> prohibited this load_balancer_check from being saved:</h2>

      <ul>
      <% @load_balancer_check.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
